{% extends 'base.html.twig' %}

{% block body %}
   <div class="container">

    <h1>Passer Reclamation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('Espace_index') }}">Retour</a>
        </li>
    </ul>
{% endblock %}
